import { Typography } from "@shared/ui";

const Loading = () => {
  return (
    <Typography variant="body" weight="bold" color="warning">
      Loading...
    </Typography>
  );
};

export default Loading;
